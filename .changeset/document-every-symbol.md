---
'flattify': patch
---

Document every exported symbol: the options interfaces (`FlattenOptions`, `UnflattenOptions`, `PathOptions`, `SetOptions`) and every overload of `flatten`, `unflatten`, `get`, `set` and `paths` now have JSDoc with examples, shown in editors and on JSR.
