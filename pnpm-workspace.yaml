allowBuilds:
  esbuild: true
  # Downloads the workerd binary used by `pnpm run test:workers`
  workerd: true

overrides:
  # GHSA-g7r4-m6w7-qqqr (transitive via vitest > vite)
  esbuild: ^0.28.1
  # GHSA-4cwx-7wf7-3272 (transitive via miniflare, which pins undici 7.28.0)
  undici: ^7.29.0
  # GHSA-rgj7-g3m4-5g8c (transitive via miniflare, which pins sharp 0.35.2)
  sharp: ^0.35.4
